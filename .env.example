DATABASE_URL="file:./dev.db"

# AI provider runtime status is exposed at /api/ai-service-status.
# The current productized loop intentionally keeps AI_PROVIDER="mock".
AI_PROVIDER="mock"
AI_PROVIDER_MANUAL_ACCEPTED="false"

# Optional future real-model integration. Do not commit real keys.
# The adapter exists behind src/server/ai/types.ts, but it stays inactive until
# AI_PROVIDER="openai", credentials are present, and AI_PROVIDER_MANUAL_ACCEPTED="true".
# OPENAI_API_KEY="sk-..."
# OPENAI_MODEL="gpt-4.1-mini"
# OPENAI_BASE_URL="https://api.openai.com/v1/chat/completions"
