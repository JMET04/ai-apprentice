interface:
  display_name: "自然沟通"
  short_description: "让每次回复更自然、体贴、直接，同时守住真实与安全边界"
  default_prompt: "Use $warm-human-communication to answer the user naturally, directly, and without canned service language."
policy:
  allow_implicit_invocation: true
